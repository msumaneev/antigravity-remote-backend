const { getProjectsTree } = require('./dist/api/handlers.js');

async function run() {
    // If it's not exported directly, maybe I have to grab it differently.
    // Actually, getProjectsTree is NOT exported!
}
run();
